[package]
name = "aura-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "aura"
path = "src/main.rs"

[dependencies]
aura-compiler = { path = "../aura-compiler" }

[features]
# AOT 编译支持（转发 aura-compiler 的 llvm feature）
llvm = ["aura-compiler/llvm"]