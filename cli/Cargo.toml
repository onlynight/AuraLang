[package]
name = "cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "aura"
path = "src/main.rs"

[dependencies]
compiler = { path = "../compiler" }

[features]
# AOT 编译支持（转发 compiler 的 llvm feature）
llvm = ["compiler/llvm"]