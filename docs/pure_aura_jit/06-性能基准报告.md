# 06 · 性能基准报告（P5）

> **文档定位**：P5 性能基准测试结果，VM / JIT / AOT 三种执行模式的性能对比
> **配套文档**：`03-分阶段开发计划.md`（P5 阶段定义）、`jit模式优化方案.md`（优化现状与目标）
> **关联分析**：`docs/JIT性能分析.md`（P5 遗留问题与 Fix A/B）
> **日期**：2026-09-13

---

## 一、测试环境

| 项目 | 配置 |
|------|------|
| **操作系统** | Windows 11 (x86_64) |
| **CPU** | 64 核（含超线程） |
| **编译器** | Aura Compiler 0.1.0-phase10 |
| **JIT 后端** | Cranelift 0.116（opt_level=speed） |
| **AOT 后端** | LLVM 23.1.0（O2 优化） |
| **测试套件** | `tests/pure_aura/three_mode_benchmarks.aura`（12 用例） |
| **Rust 基准** | `compiler/benches/jit_vs_vm_bench.rs`（5 用例） |

---

## 二、当前性能数据（P7 优化后）

### 2.1 Rust 侧基准（jit_vs_vm_bench.rs）

| 场景 | VM（解释器） | JIT（Cranelift） | AOT（LLVM） | JIT 加速比 | AOT 加速比 |
|------|-------------|------------------|-------------|-----------|-----------|
| fib(25) | 230 ms/op | **2.97 ms/op** | 5.52 ms/op | **78x** | 42x |
| sum(60000) | 55.7 ms/op | **0.51 ms/op** | 4.53 ms/op | **109x** | 12x |
| hotLoop(60000) | — | **0.48 ms/op** | — | — | — |
| fact(20) | 0.53 ms/op | **0.06 ms/op** | 4.10 ms/op | **9x** | 0.13x |
| matmul(30x30) | — | — | — | — | — |

**关键发现**：
- JIT 在 fib(25) 和 sum(60000) 两个场景中均**快于 AOT**
- fib(25)：JIT 是 AOT 的 **1.86x** 快
- sum(60000)：JIT 是 AOT 的 **8.9x** 快
- 原因：AOT 基准每次迭代启动新进程（~4ms 开销），JIT 在进程内执行
- JIT 编译延迟约 1–5ms（Cranelift speed 优化）

### 2.2 Aura 侧基准（three_mode_benchmarks.aura）

| 基准用例 | 函数 | 预期 JIT 加速比（vs VM） |
|---------|------|----------------------|
| 1. Fibonacci | `fib(20)` | 2–5× |
| 2. 阶乘 | `factorial(15)` | 2–5× |
| 3. 循环求和 | `loopSum(1000)` | 1.5–3× |
| 4. 矩阵乘法 | `matmul(30x30)` | 2–4× |
| 5. 字符串拼接 | `stringConcat(100)` | 1.2–2× |
| 6. 数组填充 | `fillArray(5000)` | 1.5–3× |
| 7. 条件分类 | `classify(1000)` | 1.3–2× |
| 8. 数学运算 | `mathOps(1000)` | 1.5–3× |
| 9. 深度递归 | `deepRecursion(2000)` | 1.5–3× |
| 10. 热点循环 | `hotLoop(50000)` | 2–5× |
| 11. 素数筛选 | `countPrimes(10000)` | 1.5–3× |
| 12. 字符串搜索 | `searchInText()` | 1.2–2× |

---

## 三、与 Java JIT 的对比

| 维度 | Aura JIT | Java JIT（C1/C2） | 差距 |
|------|---------|-------------------|------|
| **优化深度** | Cranelift 基线 | C2 深度优化（逃逸分析、去虚拟化、O3 内联） | 大 |
| **编译去重** | ❌ 无（P8 待做） | ✅ 编译锁 + 去重 | 中 |
| **OSR** | ❌ 无（P8 待做） | ✅ 成熟 | 大 |
| **类型反馈** | ❌ 无（P9 待做） | ✅ 成熟（单态/双态/泛态） | 大 |
| **多级优化** | ❌ 单级 | ✅ C1（快速）→ C2（深度） | 大 |
| **去优化** | ❌ 回退解释器 | ✅ 回退 C1 编译 | 中 |
| **Profile-Guided** | ❌ 无（P10 待做） | ✅ 成熟 | 大 |
| **编译延迟** | ~1–5ms（快） | C1 ~5–20ms / C2 ~100–500ms | ✅ Aura 胜 |
| **并发模型** | 线程独立 VM | 共享 JVM | 架构差异 |

---

## 四、并发层性能（事件驱动改造前后预估）

| 指标 | 当前（轮询） | 改造后（事件驱动） | 提升 |
|------|-------------|-------------------|------|
| **CPU 空转** | 100%（busy-spin） | 0%（epoll 阻塞） | ✅ 100% |
| **recv 延迟** | 1ms（轮询间隔） | <100µs（epoll 唤醒） | ✅ 10x |
| **select 延迟** | 1ms（轮询间隔） | <200µs（epoll 多路复用） | ✅ 5x |
| **ask 延迟** | 轮询间隔（协程调度） | <100µs（epoll 唤醒） | ✅ 10x |
| **内存开销** | 0 | ~40 字节/Channel（EventFd） | ⚠️ 增加 |
| **锁竞争** | ❌ 无 | ❌ 无（保持无锁） | ✅ 持平 |
| **死锁风险** | ❌ 无 | ❌ 无（无锁） | ✅ 持平 |

---

## 五、性能优化路线图

### 5.1 短期（P8，2-3 周）

| 指标 | 当前 | 目标 |
|------|------|------|
| 编译去重 | ❌ 无 | ✅ 全局缓存，N 线程只编译 1 次 |
| OSR | ❌ 无 | ✅ 循环体 OSR |
| 逃逸分析 | ❌ 无 | ✅ 基础逃逸分析（函数内对象） |
| sum(60000) | 0.51 ms/op | <0.3 ms/op |
| fib(25) | 2.97 ms/op | <2.0 ms/op |

### 5.2 中期（P9，4-6 周）

| 指标 | 当前 | 目标 |
|------|------|------|
| 类型反馈 | ❌ 无 | ✅ 单态/双态推测 |
| 投机内联 | ❌ 无 | ✅ 基于类型反馈 |
| 编译线程池 | ❌ 主线程编译 | ✅ 后台编译，主线程不阻塞 |
| 虚方法调用 | vtable 查找 | 直接调用（投机） |
| sum(60000) | 0.51 ms/op | <0.2 ms/op |

### 5.3 长期（P10，8-12 周）

| 指标 | 当前 | 目标 |
|------|------|------|
| Profile-Guided | ❌ 无 | ✅ 运行时 profile 驱动优化 |
| 多级优化 | ❌ 单级（C1） | ✅ 两级（C1 快速 → C2 深度） |
| 去优化（deopt） | ❌ 仅回退解释器 | ✅ 回退到 C1 编译 |
| sum(60000) | 0.51 ms/op | <0.1 ms/op |
| fib(25) | 2.97 ms/op | <1.0 ms/op |

---

## 六、已完成的优化（P5–P7）

| 阶段 | 优化项 | 位置 | 效果 |
|------|--------|------|------|
| P5 | Fix A：入口函数强制编译 | `vm/mod.rs::force_jit_compile` | sum(60000) 从 1.0x → **115x** |
| P5 | Fix B：递归函数 JIT 支持 | `vm/jit.rs` dispatch_table + call_indirect | fib(25) 从 1.0x → **91x** |
| P7 | 7 个字节码优化 pass | `vm/jit_opt.rs` | 编译质量提升 |
| P7 | Cranelift speed 优化标志 | `vm/jit.rs` `opt_level: "speed"` | 编译延迟 + 执行性能 |

### 7 个字节码优化 pass

| # | Pass | 函数 | 作用 | 典型收益 |
|---|------|------|------|----------|
| 1 | 常量折叠 | `fold_constants` | 编译期计算常量算术/比较 | 消除运行时计算 |
| 2 | 死码消除 | `eliminate_dead_code` | 删除 LoadVar/StoreVar 同槽对 | 减少指令数 10-30% |
| 3 | 跳转线程化 | `thread_jumps` | 消除「Jump 紧跟 Jump」的冗余跳转 | 简化控制流 |
| 4 | 强度削弱 | `strength_reduce` | `/2^n`→`>>n`、`*2^n`→`<<n` | 除法变移位 |
| 5 | 指令调度 | `schedule_instructions` | 独立 Load 重排，提高 ILP | 减少 CPU 停顿 |
| 6 | 函数内联 | `inline_functions` | 内联 <20 指令的小函数 | 消除调用开销 |
| 7 | 循环展开 | `unroll_loops` | 2x 展开简单循环 | 减少分支开销 |

---

## 七、测试覆盖

### 7.1 P5 测试套件

| 文件 | 测试数 | 状态 |
|------|--------|------|
| `three_mode_benchmarks.aura` | 12 基准用例 | ✅ 已创建 |
| `three_mode_tests.aura`（P4） | 12 一致性用例 | ✅ 已完成 |
| `jit_vs_vm_bench.rs`（Rust） | 5 对比基准 | ✅ 已创建 |

### 7.2 三模式一致性（P4 结果）

| 测试函数 | VM | JIT | AOT | 一致性 |
|---------|-----|-----|-----|--------|
| Fibonacci | ✅ | ✅ | ✅ | ✅ |
| 阶乘 | ✅ | ✅ | ✅ | ✅ |
| 循环求和 | ✅ | ✅ | ✅ | ✅ |
| 字符串拼接 | ✅ | ✅ | ✅ | ✅ |
| 数组操作 | ✅ | ✅ | ✅ | ✅ |
| 条件分支 | ✅ | ✅ | ✅ | ✅ |
| try/catch | ✅ | ✅ | ✅ | ✅ |
| 闭包/lambda | ✅ | ✅ | ✅ | ✅ |
| 递归函数 | ✅ | ✅ | ✅ | ✅ |
| 复杂表达式 | ✅ | ✅ | ✅ | ✅ |

---

## 八、验证命令

```bash
# P5 快速验证（仅检查文件）
powershell -File scripts\verify-p5.ps1 -Quick

# P5 完整验证（含基准运行）
powershell -File scripts\verify-p5.ps1

# Aura 侧三模式基准
aura run tests/pure_aura/three_mode_benchmarks.aura          # VM
aura run tests/pure_aura/three_mode_benchmarks.aura --jit     # JIT
aura build tests/pure_aura/three_mode_benchmarks.aura --aot   # AOT

# Rust 侧 JIT vs VM 对比
cargo run --release --features jit -p compiler --bench jit_vs_vm_bench
```

---

## 九、关键文件索引

| 文件 | 职责 |
|------|------|
| `tests/pure_aura/three_mode_benchmarks.aura` | P5 性能基准测试套件（12 用例） |
| `compiler/benches/jit_vs_vm_bench.rs` | Rust JIT vs VM vs AOT 性能对比 |
| `scripts/verify-p5.ps1` | P5 验收脚本 |
| `docs/pure_aura_jit/06-性能基准报告.md` | 本文档 |
| `compiler/src/vm/jit.rs` | Cranelift JIT 编译（白名单、编译、派发） |
| `compiler/src/vm/jit_opt.rs` | 7 个字节码优化 pass |
| `compiler/src/vm/mod.rs` | 热点计数、Fix A/B、JIT 派发接缝 |

---

## 十、总结

**P5 完成**：12 个性能基准用例 + Rust 侧 JIT vs VM 对比基准 + 性能报告 + 验收脚本。

**当前最佳性能**：
- JIT（Cranelift）：sum(60000) **0.51 ms/op**，fib(25) **2.97 ms/op**
- JIT 在部分场景超过 AOT（进程启动开销差异）
- JIT 加速比：78–109x（vs VM 解释器）

**待完成优化（P8–P10）**：
- JIT 去重编译（全局缓存）
- OSR（循环体替换）
- 逃逸分析 + 标量替换
- 类型反馈 + 投机内联
- 编译线程池
- Profile-Guided 优化
- 并发层事件驱动改造（EventNotifier + Channel/Actor）
