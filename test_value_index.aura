internal value class MyStr {
    fun toInt(): Int {
        var result: Int = 0
        var sign: Int = 1
        var i: Int = 0
        if (i < this.length && this[i] == '-') {
            sign = -1
            i = i + 1
        }
        return result * sign
    }
    
    val length: Int
}
