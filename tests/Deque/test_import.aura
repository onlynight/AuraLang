package test

import aura.lang.collection.LinkedList
import aura.lang.collection.Deque

fun main(): Int {
    var l = LinkedList<Integer>()
    l.append(1)
    println("LinkedList size=" + l.count())
    
    var d = Deque<Int>()
    println("Deque created")
    return 0
}
