// 测试：方法使用 this.x（无算术）
value class Point(val x: Int, val y: Int) {
    fun getX(): Int = this.x
}

fun main() {
    val p = Point(3, 4)
    println(p.x)       // 应为 3
    println(p.getX())  // 应为 3
}
