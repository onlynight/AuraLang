// tests/photon/P3/test_syscall_write.aura
// Phase 3.4: System call table verification test
//
// Test: @native(SYS_WRITE) generates correct syscall instruction
//
// Expected output: "Hello, World!\r\n"
//
// @native(1) = Linux write syscall
// Arguments: rdi=fd(1), rsi=buf, rdx=count
//

import aura.lang.std.String

fun main() {
    // Linux write syscall: write(fd, buf, count)
    // @native(1) fun write(fd: Int, buf: Long, count: Long): Long
    
    // String to write
    val msg: String = "Hello, World!\r\n"
    
    // Get the buffer address (simplified - in real implementation, this would be from HIR)
    // For now, this is a placeholder test
    
    // The actual syscall would be generated by the compiler:
    // mov rax, 1        ; SYS_WRITE
    // mov rdi, 1        ; STDOUT
    // mov rsi, <buf>    ; message buffer
    // mov rdx, 15       ; message length
    // syscall
    
    println(msg)
}