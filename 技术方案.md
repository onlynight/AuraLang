# Aura 语言完整技术方案

## 基于 NovaOS 的下一代系统级脚本语言

---

# 第一章：项目概述

## 1.1 项目背景

NovaOS 是一套基于 Linux 底层构建的上层 Framework 框架系统，采用 Raylib 作为渲染框架，通过微服务架构（AMS、IMS、WMS 等）提供服务，上层使用 LuaJIT 作为脚本虚拟机，应用层采用 Lua 开发，通过 FFI 调用 Raylib 等系统组件。

当前方案存在性能瓶颈和开发体验不足的问题，需要一种新的脚本语言来替代 Lua，提供：
- **C 语言级别的执行效率**
- **Python 级别的开发体验**
- **现代化语言特性**
- **与 NovaOS 架构深度集成**

## 1.2 语言定位

**Aura** 是专为 NovaOS 设计的系统级脚本语言，定位为 "LuaJIT 的完美继任者"：

- **语法层**：Kotlin 风格，零学习成本
- **执行层**：AOT + JIT 混合编译，接近 C 性能
- **生态层**：声明式包管理，去中心化依赖
- **集成层**：零开销 FFI，无缝调用 C/Raylib

## 1.3 核心设计目标

| 目标 | 实现方式 | 对标 |
|------|---------|------|
| C级执行效率 | AOT编译 + 栈分配 + 无GC | C/Rust |
| Kotlin开发体验 | 完全兼容Kotlin语法 | Kotlin |
| Python包管理 | 声明式依赖 + 去中心化 | Python/Go |
| 零开销FFI | C ABI直接调用 | Rust/C |
| 系统集成 | 原生支持NovaOS微服务 | - |

---

# 第二章：语言设计

## 2.1 语言命名

**Aura**（光环）

寓意：执行时如"光环"般高效、纯净，直抵硬件；同时暗示其"照亮"NovaOS 生态的角色。

## 2.2 设计哲学

### 核心原则
> **"语法100% Kotlin，底层100%原生"**

1. **Kotlin兼容**：类型系统、语法特性完全对齐 Kotlin
2. **性能优先**：栈分配、零成本抽象、无 GC
3. **安全可控**：空安全、类型安全、内存安全（ARC）
4. **显式优于隐式**：内存分配、错误处理、类型转换显式声明
5. **系统脚本化**：既有脚本的开发效率，又有系统级的执行效率

### 设计权衡

| 维度 | 取舍 | 理由 |
|------|------|------|
| 语法 | 完全兼容Kotlin | 降低学习成本，复用生态 |
| 内存 | ARC + 栈分配 | 避免GC暂停，确定性回收 |
| 类型 | 静态 + 类型推断 | C级性能，同时保持简洁 |
| 错误 | Result + 异常 | 兼顾安全性和易用性 |
| 并发 | Actor + 协程 | 适配微服务架构 |

---

# 第三章：语法规范

## 3.1 基础语法

### 变量声明（Kotlin风格）

```aura
// 不可变（编译期常量）
val MAX_PLAYERS = 100
val PI: Float = 3.14159f

// 可变变量
var score: Int = 0
var health: Float = 100.0f
var name = "Aura"  // 类型推断

// 空安全（编译期检查）
var nullable: String? = null
var nonNull: String = "hello"  // 不可为null

// 延迟初始化
lateinit var texture: Texture

// 懒加载
val expensive by lazy {
    loadTexture("large.png")
}
```

### 基本类型（完全对齐Kotlin）

```aura
// 整数类型
val int: Int = 100
val long: Long = 100L
val short: Short = 100
val byte: Byte = 127

// 浮点类型
val float: Float = 3.14f
val double: Double = 3.14

// 其他
val bool: Boolean = true
val char: Char = 'A'
val string: String = "Hello, $name!"

// 字面量
val hex = 0xFF
val binary = 0b1100
val underscore = 1_000_000_000L
```

### 类型转换（Kotlin风格）

```aura
val i: Int = 100
val l: Long = i.toLong()
val f: Float = i.toFloat()
val s: String = i.toString()
```

## 3.2 函数定义

### 基本函数

```aura
// 标准函数
fun add(a: Int, b: Int): Int {
    return a + b
}

// 单表达式函数
fun double(x: Int): Int = x * 2

// 无返回值
fun log(message: String) {
    println(message)
}

// 默认参数
fun createWindow(
    title: String = "Aura App",
    width: Int = 800,
    height: Int = 600
): Window {
    // ...
}

// 命名参数调用
val win = createWindow(width = 1024, height = 768)
```

### 扩展函数（零开销）

```aura
// 为String添加扩展（编译为静态函数）
fun String.isEmail(): Boolean {
    return this.contains("@") && this.contains(".")
}

fun String.toCStr(): CString {
    return CString(this)
}

// 调用
val valid = "test@email.com".isEmail()
```

### 高阶函数与Lambda

```aura
// Lambda表达式
val square = fun(x: Int): Int { return x * x }
val result = square(5)  // 25

// 简洁语法
val numbers = listOf(1, 2, 3, 4, 5)
val doubled = numbers.map(fun(x) = x * 2)

// 闭包（编译为栈上闭包结构体）
fun makeCounter(): () -> Int {
    var count = 0
    return fun(): Int {
        count++
        return count
    }
}

// 内联高阶函数（编译期展开）
inline fun <T> filter(
    list: List<T>,
    predicate: (T) -> Boolean
): List<T> {
    val result = mutableListOf<T>()
    for (item in list) {
        if (predicate(item)) {
            result.add(item)
        }
    }
    return result
}
```

## 3.3 控制流

### if 表达式

```aura
// 作为表达式返回
val max = if (a > b) a else b

// 传统形式
if (score > 100) {
    println("High score!")
} else if (score > 50) {
    println("Good job")
} else {
    println("Keep trying")
}
```

### when 表达式（比Kotlin更强大）

```aura
// 基本匹配
val result = when (x) {
    0 -> "zero"
    1 -> "one"
    2 -> "two"
    else -> "other"
}

// 范围匹配
when (score) {
    in 90..100 -> grade = 'A'
    in 80..89 -> grade = 'B'
    in 70..79 -> grade = 'C'
    else -> grade = 'F'
}

// 类型匹配（智能转换）
when (value) {
    is String -> println(value.length)  // 自动转为String
    is Int -> println(value * 2)
    is List<*> -> println(value.size)
    else -> println("Unknown type")
}

// 条件守卫
when (event) {
    is MouseClick && event.x > 0 && event.y > 0 -> handleClick(event)
    is KeyPress && event.key == Key.Escape -> shutdown()
    is TimerTick -> update()
    else -> {}
}
```

### 循环

```aura
// for循环
for (i in 0..10) {
    println(i)
}

// 迭代器
for (item in list) {
    println(item)
}

// while循环
while (running) {
    update()
    render()
}

// do-while
do {
    process()
} while (hasMore())

// break/continue
for (i in 0..100) {
    if (i % 2 == 0) continue
    if (i > 50) break
    println(i)
}
```

## 3.4 数据类与结构体

### 结构体（栈分配，对标Kotlin data class）

```aura
// 数据类 → 编译为栈分配结构体
struct Player(
    val id: Int,          // 不可变
    var name: String,     // 可变
    var x: Float,
    var y: Float,
    var health: Int = 100 // 默认值
)

// 自动生成：toString, equals, hashCode, copy
val player = Player(1, "Alice", 0f, 0f)
val copy = player.copy(name = "Bob", health = 50)

// 解构声明
val (id, name, x, y) = player

// 命名参数创建
val player2 = Player(
    id = 2,
    name = "Bob",
    x = 10f,
    y = 20f
)
```

### 枚举（带关联值）

```aura
enum Color {
    RED,
    GREEN,
    BLUE,
    CUSTOM(val r: Int, val g: Int, val b: Int)  // 带属性
}

val c = Color.CUSTOM(255, 128, 0)

// 模式匹配枚举
fun getColorName(c: Color): String {
    return when (c) {
        Color.RED -> "Red"
        Color.GREEN -> "Green"
        Color.BLUE -> "Blue"
        is Color.CUSTOM -> "Custom(${c.r}, ${c.g}, ${c.b})"
    }
}
```

### 密封类

```aura
sealed class Result<out T, out E> {
    data class Success<T>(val data: T) : Result<T, Nothing>()
    data class Error<E>(val error: E) : Result<Nothing, E>()
}
```

## 3.5 接口与实现

### 接口（零成本抽象）

```aura
// 接口定义（编译为虚表）
interface Renderable {
    fun render()
    fun bounds(): Rect
    fun zOrder(): Int = 0  // 默认实现
}

// 实现接口
class Sprite : Renderable {
    var texture: Texture
    var x: Float
    var y: Float
    
    override fun render() {
        DrawTexture(texture, x, y, Color.WHITE)
    }
    
    override fun bounds(): Rect {
        return Rect(x, y, width, height)
    }
}

// 使用（虚表调用，可内联优化）
fun drawAll(items: List<Renderable>) {
    for (item in items) {
        item.render()
    }
}
```

## 3.6 错误处理

### Result 类型（推荐）

```aura
// 返回Result
fun loadTexture(path: String): Result<Texture, Error> {
    val tex = raylib.LoadTexture(path.toCStr())
    return if (tex.isNull()) {
        Result.Error(Error("Failed to load: $path"))
    } else {
        Result.Success(tex)
    }
}

// 使用Result
val result = loadTexture("sprite.png")
when (result) {
    is Result.Success -> drawTexture(result.data)
    is Result.Error -> log(result.error.message)
}

// 链式调用（类似Rust的?操作符）
fun loadAndRender(path: String): Result<Unit, Error> {
    val tex = loadTexture(path)?  // 错误自动传播
    renderTexture(tex)
    return Result.Success(Unit)
}
```

### 异常处理（可选）

```aura
// 抛出异常
fun divide(a: Int, b: Int): Int {
    if (b == 0) throw DivideByZeroException()
    return a / b
}

// 捕获异常
try {
    val result = divide(10, 0)
    println(result)
} catch (e: DivideByZeroException) {
    log("Cannot divide by zero!")
} catch (e: Exception) {
    log("Unknown error: ${e.message}")
} finally {
    cleanup()
}
```

## 3.7 并发与异步

### 协程（编译为状态机）

```aura
// 异步函数（编译为状态机，零运行时依赖）
suspend fun fetchData(url: String): Result<Data, Error> {
    val response = await httpGet(url)  // await挂起点
    val data = parseJson(response)
    return Result.Success(data)
}

// 使用
suspend fun main() {
    val task = fetchData("https://api.example.com")
    val result = await task
    
    when (result) {
        is Result.Success -> process(result.data)
        is Result.Error -> log(result.error.message)
    }
}
```

### Actor模型（NovaOS微服务）

```aura
// Actor定义
actor WindowManager {
    // 私有状态
    private var windows: List<Window> = emptyList()
    private var focused: Int = -1
    
    // 消息处理器
    fun onCreate(config: WindowConfig): Window {
        val win = Window(config)
        windows = windows + win
        return win
    }
    
    fun onFocus(id: Int) {
        if (id < windows.size) {
            focused = id
        }
    }
    
    fun onClose(id: Int) {
        windows = windows.filter { it.id != id }
    }
}

// 使用Actor
val wm = WindowManager.spawn()
val win = wm.send(Create(WindowConfig(800, 600)))
wm.send(Focus(win.id))
```

## 3.8 泛型与集合

### 泛型

```aura
// 泛型函数
fun <T> identity(value: T): T = value

fun <T> swap(a: T, b: T): Pair<T, T> = Pair(b, a)

// 泛型类
class Box<T>(val value: T) {
    var refCount: Int = 1
}

// 泛型约束
interface Comparable<T> {
    fun compare(other: T): Ordering
}

fun <T : Comparable<T>> max(a: T, b: T): T {
    return if (a.compare(b) == Ordering.GREATER) a else b
}
```

### 集合操作（C# LINQ风格）

```aura
// 创建集合
val list: List<Int> = listOf(1, 2, 3, 4, 5)
val map: Map<String, Int> = mapOf(
    "one" to 1,
    "two" to 2,
    "three" to 3
)
val set: Set<String> = setOf("apple", "banana", "orange")

// 可变集合
val mutableList = mutableListOf(1, 2, 3)
mutableList.add(4)

val mutableMap = mutableMapOf("key" to "value")

// LINQ风格操作
val result = list
    .filter { it > 2 }
    .map { it * it }
    .take(3)
    .toList()

// 遍历
for ((key, value) in map) {
    println("$key = $value")
}

// 推导式
val squares = listOf(1..10)
    .filter { it % 2 == 0 }
    .map { it * it }
```

## 3.9 FFI与系统集成

### 调用C库（零开销）

```aura
// 声明外部函数（直接映射C ABI）
extern "c" "raylib" {
    // 函数声明
    fun DrawCircle(x: Int, y: Int, radius: Float, color: Color)
    fun DrawRectangle(x: Int, y: Int, w: Int, h: Int, color: Color)
    fun LoadTexture(path: String): Texture
    fun UnloadTexture(texture: Texture)
    fun GetFrameTime(): Float
    
    // 常量
    val WHITE: Color
    val BLACK: Color
    val RED: Color
}

// 使用（零开销，直接调用）
fun render() {
    DrawCircle(400, 300, 50f, Color.RED)
    DrawRectangle(100, 100, 200, 150, Color.WHITE)
}
```

### C++库支持（通过C包装层）

```cpp
// C++ → C包装层（手动编写）
extern "C" {
    void* engine_create() { 
        return new GameEngine(); 
    }
    
    void engine_update(void* engine, float dt) { 
        static_cast<GameEngine*>(engine)->update(dt); 
    }
    
    void engine_destroy(void* engine) {
        delete static_cast<GameEngine*>(engine);
    }
}
```

```aura
// Aura调用C包装器
extern "c" "my_engine" {
    fun engine_create(): Handle
    fun engine_update(handle: Handle, dt: Float)
    fun engine_destroy(handle: Handle)
}
```

### Rust库支持（通过C ABI导出）

```rust
// Rust库 - 导出C ABI
#[no_mangle]
pub extern "C" fn process_data(data: *const u8, len: usize) -> i32 {
    // Rust实现
    let slice = unsafe { std::slice::from_raw_parts(data, len) };
    // 处理...
    42
}
```

```aura
// Aura调用
extern "c" "my_rust_lib" {
    fun process_data(data: Pointer<Byte>, len: Int): Int
}
```

### 导出给Lua（NovaOS兼容）

```aura
// 导出函数给LuaJIT
@Export("lua")
fun getFrameTime(): Float {
    return GetFrameTime()
}

// 导出结构体
@Export("lua")
struct LuaPlayer(
    val id: Int,
    var name: String,
    var health: Int
)

// Lua中调用：
// local ft = getFrameTime()
// local p = LuaPlayer(1, "Alice", 100)
```

## 3.10 模块与包管理

### 模块导入

```aura
// 导入标准库
import std.io.println
import std.io.*
import std.math.*
import std.collections.List

// 导入外部包
import "aura-json" as json
import "github.com/user/http" as http

// 内联依赖声明（PEP 723风格）
// @depends aura-json >= 1.0
// @depends aura-http from git@github.com:user/http.git
```

### 模块导出

```aura
// math.aura
public fun add(a: Int, b: Int): Int = a + b
public fun multiply(a: Int, b: Int): Int = a * b
private fun helper() { /* 不导出 */ }

// main.aura
import math.{add, multiply}
val result = add(10, 20)
```

## 3.11 元编程

### 编译期计算

```aura
// 编译期常量
const PI = 3.14159f
const MAX_BUFFER = 1024 * 1024  // 编译期计算

// 编译期函数
@Comptime
fun factorial(n: Int): Int {
    return if (n <= 1) 1 else n * factorial(n - 1)
}

val fact5 = factorial(5)  // 编译期为120，零运行时开销
```

### 属性与注解

```aura
// 自定义注解
@Deprecated("Use newVersion() instead")
fun oldFunction() { }

@Experimental
fun newFeature() { }

@Test
fun testAdd() {
    assert(add(2, 3) == 5)
}

// 自动生成代码
@Derive(Debug, Clone, Eq)
struct Point(
    val x: Float,
    val y: Float
)
// 自动生成debug, clone, equals方法
```

---

# 第四章：类型系统

## 4.1 类型体系（完全对齐Kotlin）

| Kotlin类型 | Aura类型 | 内存大小 | 说明 |
|-----------|---------|---------|------|
| `Int` | `Int` | 4字节 | 32位整数 |
| `Long` | `Long` | 8字节 | 64位整数 |
| `Short` | `Short` | 2字节 | 16位整数 |
| `Byte` | `Byte` | 1字节 | 8位整数 |
| `Float` | `Float` | 4字节 | 单精度浮点 |
| `Double` | `Double` | 8字节 | 双精度浮点 |
| `Boolean` | `Boolean` | 1字节 | 布尔值 |
| `Char` | `Char` | 2字节 | UTF-16字符 |
| `String` | `String` | 可变 | UTF-8字符串 |
| `Any` | `Any` | - | 顶级类型 |
| `Nothing` | `Nothing` | - | 无值类型 |
| `Unit` | `Unit` | 0字节 | 无返回值 |

## 4.2 类型特性

### 空安全（编译期检查）
```aura
var nonNull: String = "hello"
var nullable: String? = null

// 安全调用
val len = nullable?.length  // 如果为null则返回null

// Elvis操作符
val result = nullable ?: "default"

// 非空断言（谨慎使用）
val forced = nullable!!
```

### 智能转换
```aura
fun process(value: Any) {
    if (value is String) {
        println(value.length)  // 自动转换为String
    }
}
```

### 类型别名
```aura
typealias PlayerId = Int
typealias Callback = (String) -> Unit
```

## 4.3 与C类型映射

| Aura类型 | C类型 | 说明 |
|---------|-------|------|
| `Int` | `int32_t` | 自动映射 |
| `Long` | `int64_t` | 自动映射 |
| `Float` | `float` | 自动映射 |
| `Double` | `double` | 自动映射 |
| `Boolean` | `bool` | 自动映射 |
| `Byte` | `uint8_t` | 自动映射 |
| `Short` | `int16_t` | 自动映射 |
| `String` | `const char*` | 自动转换 |
| `Pointer<T>` | `T*` | 显式指针 |

---

# 第五章：编译系统

## 5.1 编译模式

### 三种编译模式

```bash
# 1. 字节码模式（默认）→ 跨平台
aura build main.aura          # 生成 main.auc

# 2. AOT模式 → 极致性能
aura build main.aura --aot    # 生成原生可执行文件

# 3. JIT模式 → 开发调试
aura run main.aura            # 解释执行 + 热点编译
```

### 分层编译策略

| 场景 | 模式 | 性能 | 跨平台 | 使用场景 |
|------|------|------|--------|---------|
| 开发调试 | JIT | C的70% | ✅ | 快速迭代 |
| 应用发布 | 字节码 | C的40-50% | ✅ | 热更新脚本 |
| 系统服务 | AOT | C的95% | ❌ | AMS/IMS/WMS |
| 混合模式 | Hybrid | 核心AOT，脚本字节码 | 混合 | NovaOS应用 |

## 5.2 编译器架构

### 整体架构（Rust实现）

```
┌─────────────────────────────────────────────────────┐
│                  源码 (.aura)                        │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│              词法分析 + 语法分析                     │
│         (手写递归下降 + Pratt解析)                   │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│              AST (抽象语法树)                        │
│          + 类型信息标注                              │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│              语义分析 + 类型推断                     │
│   (类似Kotlin的局部类型推断 + 空安全检查)            │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│           HIR (高级中间表示)                         │
│         + 泛型单态化                                │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│           MIR (中级中间表示)                         │
│       + 控制流分析 + 逃逸分析                       │
└────────────────────┬────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────┐
│              优化器                                 │
│  常量折叠、内联展开、死代码消除、SIMD向量化          │
└────────────────────┬────────────────────────────────┘
                     ↓
        ┌────────────┼────────────┐
        ↓            ↓            ↓
┌───────────┐ ┌───────────┐ ┌───────────┐
│ 字节码生成│ │ LLVM后端  │ │ C后端     │
│ (.auc)    │ │ (.o/.exe) │ │ (.c)      │
└───────────┘ └───────────┘ └───────────┘
        ↓            ↓            ↓
    Aura VM    原生机器码    C编译器
```

## 5.3 编译器实现技术选型

### 实现语言：Rust

**选择理由**：
1. **内存安全**：编译器复杂的数据结构操作，Rust保证内存安全
2. **现代工具链**：Cargo集成测试、文档、依赖管理
3. **零成本抽象**：保持编译器性能
4. **丰富的生态**：`syn`、`nom`等解析库成熟
5. **社区支持**：编译器开发社区活跃

**备选方案**：C++（如果团队更熟悉）

### 关键依赖库

```toml
[dependencies]
# 命令行参数解析
clap = "4.0"

# 序列化
serde = { version = "1.0", features = ["derive"] }

# 错误处理
anyhow = "1.0"
thiserror = "1.0"

# 词法分析（手写）
# 语法分析（手写递归下降 + Pratt）

# 字节码生成
# LLVM绑定（可选）
llvm-sys = "0.10"

# 并行处理
rayon = "1.0"

# 文件操作
walkdir = "2.0"
```

## 5.4 编译流程

### 1. 词法分析

```rust
// 手写词法分析器
struct Lexer {
    source: String,
    position: usize,
    line: usize,
    column: usize,
}

impl Lexer {
    fn next_token(&mut self) -> Token {
        // 识别：关键字、标识符、字面量、运算符、分隔符
    }
}
```

### 2. 语法分析（Pratt解析器）

```rust
// Pratt解析（表达式优先）
struct Parser {
    tokens: Vec<Token>,
    current: usize,
}

impl Parser {
    // 声明解析
    fn parse_declaration(&mut self) -> Declaration {
        match self.peek() {
            Token::Fun => self.parse_function(),
            Token::Struct => self.parse_struct(),
            Token::Class => self.parse_class(),
            Token::Val => self.parse_variable(),
            // ...
        }
    }
    
    // 表达式解析（Pratt）
    fn parse_expression(&mut self, min_bp: u8) -> Expression {
        // 前缀
        let lhs = match self.next() {
            Token::Number(n) => Expression::Literal(n),
            Token::Ident(name) => Expression::Variable(name),
            // ...
        };
        
        // 中缀（根据优先级）
        while let Some(infix) = self.infix_binding_power() {
            if infix.bp < min_bp { break }
            // ...
        }
    }
}
```

### 3. 类型推断

```rust
// 基于Kotlin的局部类型推断
struct TypeInferrer {
    context: TypeContext,
    constraints: Vec<Constraint>,
}

impl TypeInferrer {
    fn infer(&mut self, expr: &Expression) -> Type {
        // 收集约束
        // 求解类型（Hindley-Milner简化版）
        // 统一类型
        // 替换变量
    }
}
```

### 4. 代码生成

```rust
// 生成LLVM IR
struct LLVMCodeGenerator {
    context: Context,
    builder: Builder,
    module: Module,
}

impl LLVMCodeGenerator {
    fn generate(&mut self, ast: &AST) -> Result<Module, Error> {
        // 1. 生成函数
        // 2. 生成结构体
        // 3. 生成FFI绑定
        // 4. 优化
        // 5. 输出目标码
    }
}
```

---

# 第六章：内存管理

## 6.1 内存管理策略

### 三层内存管理

```
┌─────────────────────────────────────────────┐
│          第一层：栈分配（默认）               │
│   小对象、局部变量、短生命周期数据            │
│   开销：零                                   │
├─────────────────────────────────────────────┤
│          第二层：自动引用计数（ARC）          │
│   堆分配对象、共享数据                       │
│   开销：3-5%                                │
├─────────────────────────────────────────────┤
│          第三层：手动管理（可选）             │
│   特殊场景（C库互操作、极致性能）             │
│   开销：手动控制                            │
└─────────────────────────────────────────────┘
```

### 分配策略

```aura
// 1. 默认：栈分配
val point = Point(10f, 20f)  // 栈上分配

// 2. 显式堆分配（需要引用计数）
val shared = box Texture("sprite.png")  // 堆分配，ARC管理

// 3. 手动管理（FFI场景）
val raw = malloc(1024)  // C风格手动分配
defer { free(raw) }     // 显式释放
```

## 6.2 生命周期管理

### 编译期分析

```rust
struct LifetimeAnalyzer {
    // 分析变量生命周期
    // 确定对象能否栈分配
    // 插入ARC增减操作
}

impl LifetimeAnalyzer {
    fn analyze(&mut self, ast: &AST) -> Result<LifetimeInfo, Error> {
        // 1. 变量引用分析
        // 2. 逃逸分析
        // 3. 生命周期计算
        // 4. 优化决策
    }
}
```

### 引用计数优化

```aura
// 编译器自动优化ARC
fun process() {
    val a = box Data(100)  // 引用计数 = 1
    val b = a              // 引用计数 = 2 (复制)
    // 使用a, b
    // 离开作用域，引用计数减为0，自动释放
}
```

---

# 第七章：执行引擎

## 7.1 Aura虚拟机（字节码模式）

### 虚拟机架构

```
┌─────────────────────────────────────────────┐
│             Aura VM                          │
├─────────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐    │
│  │      字节码加载器 (.auc)             │    │
│  └──────────────┬──────────────────────┘    │
│                 ↓                           │
│  ┌─────────────────────────────────────┐    │
│  │      解释器（极致优化）              │    │
│  │   - 直接线程式（Direct Threading）   │    │
│  │   - 寄存器式VM                      │    │
│  └──────────────┬──────────────────────┘    │
│                 ↓                           │
│  ┌─────────────────────────────────────┐    │
│  │      JIT编译器（热点检测）           │    │
│  │   - 方法级JIT                      │    │
│  │   - 跟踪JIT                        │    │
│  └─────────────────────────────────────┘    │
├─────────────────────────────────────────────┤
│           运行时支持                         │
│  - FFI调度器                                │
│  - 垃圾回收（引用计数）                      │
│  - 协程调度器                               │
│  - Actor消息队列                            │
└─────────────────────────────────────────────┘
```

### 字节码设计

```rust
// 字节码指令集（精简）
enum OpCode {
    // 加载/存储
    LoadConst(i64),
    LoadVar(u16),
    StoreVar(u16),
    
    // 算术
    Add,
    Sub,
    Mul,
    Div,
    Rem,
    
    // 控制流
    Jump(i32),
    JumpIfTrue(i32),
    JumpIfFalse(i32),
    Call(u16),
    CallNative(u16),
    Return,
    
    // 对象
    NewObject(u16),
    NewArray,
    GetField(u16),
    SetField(u16),
    
    // 引用计数
    IncRef,
    DecRef,
    
    // FFI
    CallC(u16),
}
```

## 7.2 JIT编译

### JIT策略

```rust
struct JITCompiler {
    // 热点检测
    profiler: Profiler,
    // 优化编译器
    optimizer: Optimizer,
    // 代码生成
    codegen: NativeCodeGenerator,
}

impl JITCompiler {
    fn compile_hotspot(&mut self, method: &Method) -> Result<(), Error> {
        // 1. 检测热点（方法调用超过阈值）
        // 2. 编译为机器码（使用Cranelift）
        // 3. 替换字节码解释（Patch）
        // 4. 应用优化（内联、去虚拟化）
    }
}
```

### 优化技术

1. **方法内联**：热点小函数直接内联展开
2. **去虚拟化**：单态化接口调用
3. **逃逸分析**：堆分配优化为栈分配
4. **循环展开**：热点循环优化
5. **SIMD向量化**：自动使用SIMD指令

---

# 第八章：包管理系统

## 8.1 设计理念

### 核心原则

1. **声明式依赖**：在源码中直接声明
2. **去中心化**：基于Git仓库，无需中央服务器
3. **版本管理**：语义化版本（Semantic Versioning）
4. **缓存机制**：本地缓存，避免重复下载

## 8.2 依赖声明（PEP 723风格）

```aura
// ===== main.aura =====
// @depends aura-json >= 1.0
// @depends aura-http from git@github.com:user/http.git
// @depends aura-raylib == 5.0

import json
import http
import raylib.*

// 使用依赖
val data = json.parse("{\"key\": \"value\"}")
val response = http.get("https://api.example.com")
```

### 依赖格式

```aura
// 版本约束
// @depends package-name version-constraint

// 版本约束格式：
// >= 1.0    : 大于等于1.0
// == 1.2    : 精确版本1.2
// ~> 1.0    : 1.0 <= version < 2.0
// ^1.0       : 兼容版本（Maven风格）

// 来源指定
// @depends package-name from source-url

// 组合
// @depends aura-http == 2.1 from git@github.com:user/http.git
```

## 8.3 包管理器工具

```bash
# 安装依赖
aura install              # 安装所有依赖

# 更新依赖
aura update               # 更新到最新兼容版本
aura update --all         # 更新所有依赖

# 查看依赖
aura deps                 # 查看依赖树
aura deps --outdated      # 查看过时依赖

# 发布包
aura publish              # 发布到Git仓库

# 创建新包
aura new my
```