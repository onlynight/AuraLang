target/

# insta 快照测试：不匹配时生成的待审文件，不应提交
*.snap.new
build_log.txt
disasm_full.txt
*.log
log/
*.auc
*.ll
*.o
*.obj
*.exe
build/
.tmp-tmtest/
output/
__pycache__/
libs/
.migration*/
.mimocode/

# 冻结的自举编译器发布产物（Stage-0 载体，随仓库分发）
# 说明：上方 `*.exe` 仍忽略其余可执行文件，此处用 `!` 例外放行发布目录
!dist/bootstrap/aura-compiler.exe
!aura/seed/aura.exe
rust/

# AOT 运行时 LLVM IR（libc/crt/args/types/exception/channel 声明）。
# `Runtime.aura::runtimeDeclarationsExEx` 在**运行期**从磁盘读取这些文件并
# 注入模块头部；上方 `*.ll` 会忽略它们（曾因此在 d497f28 后整批丢失），
# 故此处显式放行，必须随仓库分发。
!aura/compiler/aura/lang/compiler/aot/runtime/*.ll