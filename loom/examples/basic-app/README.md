# 最小应用示例

最简 aura-loom 项目，演示基本构建流程。

## 目录结构

```text
basic-app/
├── aura.toml       # 项目配置
├── src/
│   └── main.aura   # 入口文件
└── test/
    └── main_test.aura  # 测试文件
```

## 使用

```bash
cd basic-app
loom build
loom test
loom run
```
