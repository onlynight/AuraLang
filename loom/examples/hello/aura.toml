# hello 项目配置
# 未声明的字段使用 loom 内置默认值（见 loom/src/manifest/default.toml）
# 优先级：CLI > Profile > 本文件 > 内置默认 > serde 字段默认

name = "hello"
version = "0.1.0"
description = "Hello World 示例项目"

# 只覆盖与默认不同的字段：禁用 stdlib/test-harness 插件
[plugins]
aura-stdlib = false
aura-test-harness = false
