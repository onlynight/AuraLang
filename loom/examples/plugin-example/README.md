# 插件示例

演示自定义插件开发。

## 目录结构

```text
plugin-example/
├── aura.toml
├── plugins/
│   └── custom.plugin     # 外部插件（.so/.dll）
├── src/
│   └── main.aura
└── docs/
    └── plugin-api.md     # 插件 API 文档
```
